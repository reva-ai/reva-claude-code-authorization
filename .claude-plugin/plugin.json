{
  "name": "reva-governance",
  "version": "1.0.0",
  "description": "Reva Cedar-based authorization gateway \u2014 every Claude Code tool call and prompt is checked against the Reva PDP before it runs",
  "author": {
    "name": "Reva AI"
  },
  "userConfig": {
    "auth_token": {
      "type": "string",
      "title": "Reva Auth Token",
      "description": "Authenticates every request this plugin makes to Reva \u2014 PDP evaluation (every tool call and prompt) and entity ingestion at session start. Issued by Reva for your tenant.",
      "sensitive": true,
      "required": true
    },
    "host": {
      "type": "string",
      "title": "Reva Host",
      "description": "Your Reva tenant's API host, for example api.example.reva.ai. Leave blank to use the default, api.reva.ai.",
      "required": false
    }
  }
}
